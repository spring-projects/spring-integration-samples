<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:int-amqp="http://www.springframework.org/schema/integration/amqp"
	xmlns:rabbit="http://www.springframework.org/schema/rabbit"
	xmlns:int-stream="http://www.springframework.org/schema/integration/stream"
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="http://www.springframework.org/schema/integration/amqp http://www.springframework.org/schema/integration/amqp/spring-integration-amqp.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.0.xsd
		http://www.springframework.org/schema/integration/stream http://www.springframework.org/schema/integration/stream/spring-integration-stream.xsd
		http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<bean class="org.springframework.integration.samples.amqp.RetryBeanPostProcessor" />

	<int:channel id="toRabbit" />

	<int-amqp:outbound-gateway request-channel="toRabbit" reply-channel="reply"
		amqp-template="amqpTemplate"
		exchange-name="si.test.exchange" routing-key="si.test.binding" />

	<int:channel id="reply">
		<int:queue/>
	</int:channel>

	<int-amqp:inbound-gateway request-channel="fromRabbit" task-executor="inbound-exec1"
		queue-names="si.test.queue" connection-factory="connectionFactory" />

    <int:channel id="fromRabbit" />

	<int-amqp:outbound-gateway request-channel="fromRabbit"
		amqp-template="amqpTemplate2"
		exchange-name="si.test.exchange" routing-key="si.test.binding2" />

	<int-amqp:inbound-gateway request-channel="fromRabbit2"  task-executor="inbound-exec2"
		queue-names="si.test.queue2" connection-factory="connectionFactory" />

    <int:channel id="fromRabbit2" />

	<int-amqp:outbound-gateway request-channel="fromRabbit2"
		amqp-template="amqpTemplate3"
		exchange-name="si.test.exchange" routing-key="si.test.binding3" />

	<int-amqp:inbound-gateway request-channel="fromRabbit3" task-executor="inbound-exec3"
		queue-names="si.test.queue3" connection-factory="connectionFactory" />

	<int:channel id="fromRabbit3" />

	<int:service-activator input-channel="fromRabbit3" expression="'echo:' + payload"/>

	<!-- Infrastructure -->

	<rabbit:connection-factory id="connectionFactory" addresses="legolas,localhost" />

	<rabbit:template id="amqpTemplate" connection-factory="connectionFactory" reply-queue="si.test.replies">
		<rabbit:reply-listener task-executor="reply-exec" />
	</rabbit:template>

	<rabbit:template id="amqpTemplate2" connection-factory="connectionFactory" reply-queue="si.test.replies2">
		<rabbit:reply-listener task-executor="reply-exec2" />
	</rabbit:template>

	<rabbit:template id="amqpTemplate3" connection-factory="connectionFactory" reply-queue="si.test.replies3">
		<rabbit:reply-listener task-executor="reply-exec3" />
	</rabbit:template>

	<task:executor id="reply-exec" />
	<task:executor id="reply-exec2" />
	<task:executor id="reply-exec3" />

	<task:executor id="inbound-exec1" />
	<task:executor id="inbound-exec2" />
	<task:executor id="inbound-exec3" />

	<rabbit:admin connection-factory="connectionFactory" />

	<rabbit:queue-arguments id="haArgs">
		<entry key="x-ha-policy" value="all" />
	</rabbit:queue-arguments>

	<rabbit:queue name="si.test.queue" queue-arguments="haArgs" />

	<rabbit:queue name="si.test.queue2" queue-arguments="haArgs" />

	<rabbit:queue name="si.test.queue3" queue-arguments="haArgs" />

	<rabbit:queue name="si.test.replies" queue-arguments="haArgs" />

	<rabbit:queue name="si.test.replies2" queue-arguments="haArgs" />

	<rabbit:queue name="si.test.replies3" queue-arguments="haArgs" />

	<rabbit:direct-exchange name="si.test.exchange">
		<rabbit:bindings>
			<rabbit:binding queue="si.test.queue" key="si.test.binding" />
			<rabbit:binding queue="si.test.queue2" key="si.test.binding2" />
			<rabbit:binding queue="si.test.queue3" key="si.test.binding3" />
		</rabbit:bindings>
    </rabbit:direct-exchange>

</beans>
